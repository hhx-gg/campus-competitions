# 项目维护约定

- 每次构建前检查旧版安装包、重复导出文件和临时目录。
- 用户可运行文件只保留当前版本：`releases/杜绝信息差_0.6.1_x64-setup.exe`；发布脚本会删除旧版安装包。
- `dist/` 与 `src-tauri/target/` 属于可重建构建产物，不复制到其他位置；构建会覆盖当前产物。
- `node_modules/`、`.vs/` 等工具缓存保留在本机但不纳入项目交付，也不上传 GitHub。
- 删除前先确认路径和内容，只清理明确属于本项目的空目录、旧安装包或临时文件。
- `.release-secrets/` 只保存本机升级签名私钥，不进入 Git、不复制进安装包；丢失私钥后旧客户端将无法信任新升级包。
